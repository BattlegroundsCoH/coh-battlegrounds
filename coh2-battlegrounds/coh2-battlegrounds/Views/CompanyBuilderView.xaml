<bg:ViewState  xmlns:bg="clr-namespace:BattlegroundsApp" 
               x:Class="BattlegroundsApp.Views.CompanyBuilderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BattlegroundsApp.Views"
             xmlns:card="clr-namespace:BattlegroundsApp.Controls.CompanyBuilderControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="1000" Background="white">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Row 0 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding CompanyName}" Margin="2.5"/>

        <!-- For company size use the size variable from Company.cs file -->
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="2.5">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}Company Size: {0}/{1}">
                    <Binding Path="CompanySize" Mode="TwoWay"/>
                    <Binding Path="CompanyMaxSize"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>

        <!-- Row 1 -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Infantry"/>
        
        <TextBlock Grid.Row="1" Grid.Column="1" Text="Support Weapons"/>
        
        <TextBlock Grid.Row="1" Grid.Column="2" Text="Vehicles"/>

        <TextBlock Grid.Row="1" Grid.Column="3" Text="Abilities"/>
        
        <!-- Row 2 -->
        <!-- Infantry -->
        <ScrollViewer Grid.Row="2" Grid.Column="0" CanContentScroll="False" VerticalScrollBarVisibility="Visible" Height="575" MaxHeight="575">
            <StackPanel x:Name="InfantryList">
                
            </StackPanel>
        </ScrollViewer>

        <!-- Support -->
        <ScrollViewer Grid.Row="2" Grid.Column="1" CanContentScroll="False" VerticalScrollBarVisibility="Visible" Height="575" MaxHeight="575">
            <StackPanel x:Name="SupportList">

            </StackPanel>
        </ScrollViewer>

        <!-- Vehicles -->
        <ScrollViewer Grid.Row="2" Grid.Column="2" CanContentScroll="False" VerticalScrollBarVisibility="Visible" Height="575" MaxHeight="575">
            <StackPanel x:Name="VehicleList">

            </StackPanel>
        </ScrollViewer>

        <!-- Abilities -->
        <ScrollViewer Grid.Row="2" Grid.Column="3" CanContentScroll="False" VerticalScrollBarVisibility="Visible" Height="575" MaxHeight="575">
            <StackPanel x:Name="AbilityList">

            </StackPanel>
        </ScrollViewer>

        <!-- Row 3 -->
        <Button x:Name="saveButton" Grid.Row="3" Grid.Column="0" Content="Save" Margin="2.5" Width="125" Click="SaveButton_Click"/>

        <Button x:Name="backButton" Grid.Row="3" Grid.Column="1" Content="Back" Margin="2.5" Width="125" Click="BackButton_Click"/>

    </Grid>
</bg:ViewState>
