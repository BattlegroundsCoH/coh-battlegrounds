<UserControl x:Class="Battlegrounds.Views.MultiplayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Battlegrounds.Views" 
             xmlns:converter="clr-namespace:Battlegrounds.Converters" 
             xmlns:viewmodels="clr-namespace:Battlegrounds.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MultiplayerViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converter:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converter:JoinableStatusConverter x:Key="JoinableStatusConverter"/>
        <converter:ScenarioIdToNameConverter x:Key="ScenarioIdToNameConverter"/>

        <Style x:Key="LobbyListView" TargetType="ListView">
            <Setter Property="Background" Value="{StaticResource Brush.Surface.Card}"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Hairline}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListView">
                        <Border 
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="0">
                            <ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LobbyListViewItem" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        CornerRadius="0"
                        BorderBrush="{StaticResource Brush.Border.Hairline}"
                        BorderThickness="0">
                            <ContentPresenter 
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Margin="{TemplateBinding Padding}"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Accent.Gold}"/>
                                <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Border.Hairline}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom lobby row template -->
        <DataTemplate x:Key="LobbyItemTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Lock icon (only shown when password protected) -->
                <TextBlock Text="🔒"
                           FontSize="13"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"
                           Foreground="{StaticResource Brush.Text.Dim}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPasswordProtected}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Lobby name and map/mode -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               TextTrimming="CharacterEllipsis"/>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <TextBlock FontSize="11"
                                   Foreground="{StaticResource Brush.Text.Dim}"
                                   TextTrimming="CharacterEllipsis">
                            <TextBlock.Text>
                                <MultiBinding Converter="{StaticResource ScenarioIdToNameConverter}">
                                    <Binding Path="Map"/>
                                    <Binding Path="Game"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Text=" · "
                                   FontSize="11"
                                   Foreground="{StaticResource Brush.Text.Dim}"/>
                        <TextBlock Text="{Binding GameMode}"
                                   FontSize="11"
                                   Foreground="{StaticResource Brush.Text.Dim}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Player count badge -->
                <Border Grid.Column="2"
                        CornerRadius="0"
                        Padding="8,3"
                        Margin="10,0,0,0"
                        VerticalAlignment="Center">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="{StaticResource Brush.Border.Hairline}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CanJoin}" Value="False">
                                    <Setter Property="Background" Value="{StaticResource Brush.Status.Danger}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding Players}"
                               Foreground="{StaticResource Brush.Text.Primary}"
                               FontSize="11"
                               FontWeight="SemiBold"/>
                </Border>
            </Grid>
        </DataTemplate>

    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Control Panel -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="AVAILABLE MATCHES" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Foreground="{StaticResource Brush.Text.Primary}"
                   VerticalAlignment="Center"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button 
                    Content="Refresh" 
                    Width="100"
                    Command="{Binding RefreshLobbies}"
                    Style="{StaticResource Button.Primary}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                <Button 
                    Content="Create Lobby" 
                    Margin="10,0,0,0"
                    Width="120" 
                    Command="{Binding CreateLobby}"
                    Style="{StaticResource Button.Primary}"
                    IsEnabled="{Binding IsConnected}"/>
            </StackPanel>
        </Grid>

        <!-- Main Content: Lobby list + Preview panel -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- Lobby List -->
            <ListView 
                Grid.Column="0"
                ItemsSource="{Binding Lobbies}"
                SelectedItem="{Binding SelectedLobby, Mode=TwoWay}"
                Style="{StaticResource LobbyListView}"
                ItemContainerStyle="{StaticResource LobbyListViewItem}"
                ItemTemplate="{StaticResource LobbyItemTemplate}"
                IsEnabled="{Binding IsConnected}"
                BorderThickness="0"
                Margin="0,0,10,0"/>

            <!-- Preview panel -->
            <local:LobbyPreviewView Grid.Column="1" DataContext="{Binding}"/>
        </Grid>

        <!-- Bottom Status Bar -->
        <StatusBar Grid.Row="2" Height="30" 
               Background="{StaticResource Brush.Surface.Sunken}"
               Foreground="{StaticResource Brush.Text.Primary}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage, FallbackValue=Connecting...}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding PlayersOnline, StringFormat='Players Online: {0}', FallbackValue='Players Online: 0'}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Loading Overlay -->
        <Grid 
            Grid.RowSpan="3" 
            Background="{StaticResource Brush.Surface.Scrim}" 
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressBar IsIndeterminate="True" 
                         Style="{StaticResource ProgressBar.Default}"
                 Width="200" 
                 Height="4"/>
        </Grid>

    </Grid>
</UserControl>
